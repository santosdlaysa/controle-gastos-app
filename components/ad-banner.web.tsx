// Stub para web: react-native-google-mobile-ads importa internals nativos do
// React Native que não existem no bundle web. O Metro resolve este arquivo
// (.web.tsx) automaticamente na plataforma web, evitando o import do módulo
// de anúncios. No app nativo, ad-banner.tsx é usado normalmente.
export function AdBanner() {
  return null;
}
